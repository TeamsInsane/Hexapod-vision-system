[project]
name = "hexapod-vision-system"
version = "0.1.0"
description = "AprilTag-based vision, aiming, calibration, and evaluation tools for a hexapod robot"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.10.8",
    "numpy>=2.0.2",
    "opencv-python>=5.0.0.93",
]
